{
  "buildCommand": "npm install -g pnpm@11 && pnpm install --frozen-lockfile && pnpm --filter @workspace/site run build",
  "outputDirectory": "artifacts/site/dist/public",
  "framework": null,
  "rewrites": [{ "source": "/(.*)", "destination": "/index.html" }],
  "env": {
    "BASE_PATH": "/",
    "PORT": "3000"
  }
}
